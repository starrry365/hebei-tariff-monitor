# 移动资费专区 · 栏目全覆盖核对（按用户 URL 实参数重跑）

> 复核时间：2026-09-23 22:56
> 数据来源：`https://h.app.coc.10086.cn/cmcc-app/pc-pages/tariffZonePers.html?pageId=834148205904408576&prov=311&channelId=P00000010686`
> 采集方式：复用 `tariff_monitor.fetch_all`（含声明对账守卫 + 两容器收音 `nonModuleList`/`moduleList` + `fistLimit=5000`），仅把 Referer 换成用户当前 URL 的 channelId。
> 结论：**总计 5375 条，声明对账缺口 = 0**；channelId 不影响数据源（与生产 channelId=…132579 结果逐位一致）。

## 一、页面两级栏目 → 数据覆盖对照

页面是「全网资费 / 河北资费」两大板块，各板块下个人/政企两层，页签内分套餐/加装包/营销活动/港澳台国际/标准资费。

### 全网资费（attr=1）
| 栏目 | 个人（type1=1） | 政企（type1=2） | 采集判定 |
|---|---|---|---|
| 套餐 | 17 系列 / 75 条 | — | ✅ 全 |
| 加装包 | 262 系列 / 1583 条 | 2 系列 / 2 条 | ✅ 全 |
| 营销活动 | 5 系列 / 18 条 | — | ✅ 全 |
| 港澳台/国际资费 | 34 系列 / 1318 条 | — | ✅ 全（注意：**1318 条全部来自 moduleList 容器**，非 moduleList 恒空） |
| 标准资费 | 0 | 0 | 上游无（attr=1 分类树不含 type2=5） |

### 河北资费（attr=2）
| 栏目 | 个人 | 政企 |
|---|---|---|
| 套餐 | 429 系列 / 526 条 | 4 系列 / 4 条 |
| 加装包 | 682 系列 / 695 条 | 11 系列 / 11 条 |
| 营销活动 | 1038 系列 / 1048 条 | 7 系列 / 7 条 |
| 港澳台/国际资费 | 3 系列 / 3 条 | — |
| 标准资费 | 85 系列 / 85 条 | — |

**河北各栏目全部取全（total=系列数 完全对上），声明对账缺口 0。**

### attr=3
5 个组合全部 0 条 —— 是上游**确实无数据**（attr=3 对应后端「未启用」板块，页面上不展示），非采集遗漏。已在探针空栏自检里标记，但不会告警（因为系列数也是 0）。

---

## 二、关键判定维度

1. **栏目分类树**（getType2List）= 18 个 (attr,type1,type2) 组合，与页面实际展示一致，无遗漏枚举。
2. **声明对账**（每系列 `nonModuleListTotal + moduleListTotal` vs 实取）= **0 缺口** ⇒ 不存在第三种容器 / fistLimit 截断。
3. **空壳检测**：网上港澳台（attr=1/t2=4）旧版曾整栏既报「有系列无明细」，本次已用 moduleList 容器补上，1318 条全取回。
4. **channelCount 无关**：用户 URL 的 channelCount=…686 与生产 channel 同结果，数据源由后端分类树决定，channelCount 只影响前端导航态。

---

## 三、仍要注意的两件关联事项

- **公告**：移动资费专区首页不发公告请求，公告在河北这套接口里也没有非空来源（此前结论：只有北京/集团 provC=100 有 264 条，河北 311 为 0）。页面上的「公告」要么是后端第 app 层加密通道、要么是本地缓存。
- **attr=3** 是「未启用板块」，页面不展示，不是我们需要采集的栏目。

---

## 复现方式

```bash
cd .tools/hebei-tariff-monitor
python probes/probe_channel_full.py   # 已固定写入用户 URL 的 channelCount
```